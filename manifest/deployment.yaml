apiVersion: apps/v1
kind: Deployment
metadata:
  name: springboot-app-agil
  labels:
    app: springboot-app-agil
spec:
  replicas: 2
  selector:
    matchLabels:
      app: springboot-app-agil
  template:
    metadata:
      labels:
        app: springboot-app-agil
    spec:
      containers:
      - name: ct-app-agil
        image: agil1106/springboot-app:latest 
        ports:
        - containerPort: 8080
---
apiVersion: v1
kind: Service
metadata:
  name: springboot-app-agil
  labels:
    app: springboot-app-agil
spec:
  type: ClusterIP
  selector:
    app: springboot-app-agil
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: springboot-app-agil
  labels:
    app: springboot-app-agil
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /
spec:
  ingressClassName: nginx
  rules:
  - host: appsku.local
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: springboot-app-agil
            port:
              number: 80